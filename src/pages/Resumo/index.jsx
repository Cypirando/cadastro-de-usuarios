import "./Resumo.css";

const Resumo = () => {
  return (
    <div className="form-resumo">
      {/* <table>
        <thead>
          <tr>
            <th>Campo</th>
            <th>Valor</th>
          </tr>
        </thead>
        <tbody>
          {Object.keys(user).map((key) => (
            <tr key={key}>
              <td>{key}</td>
              <td>{user[key]}</td>
            </tr>
          ))}
        </tbody>
      </table> */}
    </div>
  );
};

export default Resumo ;
